import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',

    primary: {
      main: '#091D9E',
    },
    secondary: {
      main: '#BCC5FF',
    },
  },
});

export default theme;
